# bmi-classification-ml
Machine learning project that predicts BMI categories using gender, height, and weight. Includes data preprocessing, model comparison across logistic regression, random forest, and SVM, and evaluation with cross validation to identify the most accurate classifier.
